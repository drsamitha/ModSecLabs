# The engine core needs only numpy (PCA/anomaly math).
numpy>=1.24
# Optional tiers:
Flask>=3.0        # webapp/ dashboard
requests>=2.31    # trafficgen/ (in-container)
urllib3>=2.0      # trafficgen/
torch>=2.0        # OPTIONAL: --autoencoder tier (falls back to numpy PCA if absent)
anthropic>=0.40   # OPTIONAL: analyze.py --claude (live lockdown generation)
